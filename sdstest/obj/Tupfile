# 1. Build the source code in C
# will grab the libraries included
# in the include definitions
# and call the !cc macro to build

include_rules


ifeq (@(DO_RELEASE),y)
: foreach ../src/*.c |> !ccorel |>
else
: foreach ../src/*.c |> !ccodbg |>
endif
